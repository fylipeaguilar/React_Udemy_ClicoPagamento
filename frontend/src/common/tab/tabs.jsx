//Componente que vai agrupar todas as nossas abas
// Importando o React
import React from 'react'

export default props => (

    <div className='nav-tabs-custom'>
        {props.children}
    </div>

)